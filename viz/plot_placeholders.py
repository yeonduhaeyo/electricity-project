from utils.ui_components import placeholder

def hourly_prediction():
    # 나중에 Plotly figure로 교체
    return placeholder("여기에 24시간 예측 라인 + 신뢰구간 밴드가 표시됩니다.")

def usage_rate_trend():
    return placeholder("여기에 사용량(kWh) 막대 + 평균단가(원/kWh) 라인(복축) 트렌드가 표시됩니다.")

def weekday_hour_heatmap():
    return placeholder("여기에 요일×시간대 평균 단가 히트맵이 표시됩니다.")
